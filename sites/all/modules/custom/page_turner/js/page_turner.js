/**
 * Page Turner hides/shows parts of a node
 * based on settings about character length
 * Here's where all the display work is done
 *
 **/

if (Drupal.settings.page_turner_active) {
  console.log("I loaded!");
}
